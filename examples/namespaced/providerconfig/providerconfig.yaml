apiVersion: maas.m.crossplane.io/v1beta1
kind: ProviderConfig
metadata:
  name: default
  namespace: crossplane-system
spec:
  apiURL: "http://localhost:5240/MAAS"
  apiVersion: "2.0"
  credentials:
    source: Secret
    secretRef:
      name: maas-creds
      namespace: crossplane-system
      key: credentials
