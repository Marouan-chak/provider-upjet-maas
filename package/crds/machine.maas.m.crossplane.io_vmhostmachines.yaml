---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: vmhostmachines.machine.maas.m.crossplane.io
spec:
  group: machine.maas.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - maas
    kind: VMHostMachine
    listKind: VMHostMachineList
    plural: vmhostmachines
    singular: vmhostmachine
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VMHostMachine is the Schema for the VMHostMachines API. Provides
          a resource to manage MAAS VM host machines.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VMHostMachineSpec defines the desired state of VMHostMachine
            properties:
              forProvider:
                properties:
                  cores:
                    description: |-
                      (Number) The number of CPU cores (defaults to 1).
                      The number of CPU cores (defaults to 1).
                    type: number
                  domain:
                    description: |-
                      (String) The VM host machine domain. This is computed if it's not set.
                      The VM host machine domain. This is computed if it's not set.
                    type: string
                  hostname:
                    description: |-
                      (String) The VM host machine hostname. This is computed if it's not set.
                      The VM host machine hostname. This is computed if it's not set.
                    type: string
                  memory:
                    description: |-
                      (Number) The VM host machine RAM memory, specified in MB (defaults to 2048).
                      The VM host machine RAM memory, specified in MB (defaults to 2048).
                    type: number
                  networkInterfaces:
                    description: |-
                      as-blocks mode. (see below for nested schema)
                      A list of network interfaces for new the VM host. This argument only works when the VM host is deployed from a registered MAAS machine. Parameters defined below. This argument is processed in [attribute-as-blocks mode](https://www.io/docs/configuration/attr-as-blocks.html).
                    items:
                      properties:
                        fabric:
                          description: |-
                            (String) The fabric for the network interface.
                            The fabric for the network interface.
                          type: string
                        ipAddress:
                          description: |-
                            (String) Static IP configured on the new network interface.
                            Static IP configured on the new network interface.
                          type: string
                        name:
                          description: |-
                            (String) The network interface name.
                            The network interface name.
                          type: string
                        subnetCidr:
                          description: |-
                            (String) The subnet CIDR for the network interface.
                            The subnet CIDR for the network interface.
                          type: string
                        vlan:
                          description: |-
                            (String) The VLAN for the network interface.
                            The VLAN for the network interface.
                          type: string
                      type: object
                    type: array
                  pinnedCores:
                    description: |-
                      (Number) List of host CPU cores to pin the VM host machine to. If this is passed, the cores parameter is ignored.
                      List of host CPU cores to pin the VM host machine to. If this is passed, the `cores` parameter is ignored.
                    type: number
                  pool:
                    description: |-
                      (String) The VM host machine pool. This is computed if it's not set.
                      The VM host machine pool. This is computed if it's not set.
                    type: string
                  storageDisks:
                    description: |-
                      as-blocks mode. (see below for nested schema)
                      A list of storage disks for the new VM host. Parameters defined below. This argument is processed in [attribute-as-blocks mode](https://www.io/docs/configuration/attr-as-blocks.html).
                    items:
                      properties:
                        pool:
                          description: |-
                            (String) The VM host machine pool. This is computed if it's not set.
                            The VM host storage pool name.
                          type: string
                        sizeGigabytes:
                          description: |-
                            (Number) The storage disk size, specified in GB.
                            The storage disk size, specified in GB.
                          type: number
                      type: object
                    type: array
                  vmHost:
                    description: |-
                      (String) ID or name of the VM host used to compose the new machine.
                      ID or name of the VM host used to compose the new machine.
                    type: string
                  vmHostRef:
                    description: Reference to a VMHost in machine to populate vmHost.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  vmHostSelector:
                    description: Selector for a VMHost in machine to populate vmHost.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  zone:
                    description: |-
                      (String) The VM host machine zone. This is computed if it's not set.
                      The VM host machine zone. This is computed if it's not set.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  cores:
                    description: |-
                      (Number) The number of CPU cores (defaults to 1).
                      The number of CPU cores (defaults to 1).
                    type: number
                  domain:
                    description: |-
                      (String) The VM host machine domain. This is computed if it's not set.
                      The VM host machine domain. This is computed if it's not set.
                    type: string
                  hostname:
                    description: |-
                      (String) The VM host machine hostname. This is computed if it's not set.
                      The VM host machine hostname. This is computed if it's not set.
                    type: string
                  memory:
                    description: |-
                      (Number) The VM host machine RAM memory, specified in MB (defaults to 2048).
                      The VM host machine RAM memory, specified in MB (defaults to 2048).
                    type: number
                  networkInterfaces:
                    description: |-
                      as-blocks mode. (see below for nested schema)
                      A list of network interfaces for new the VM host. This argument only works when the VM host is deployed from a registered MAAS machine. Parameters defined below. This argument is processed in [attribute-as-blocks mode](https://www.io/docs/configuration/attr-as-blocks.html).
                    items:
                      properties:
                        fabric:
                          description: |-
                            (String) The fabric for the network interface.
                            The fabric for the network interface.
                          type: string
                        ipAddress:
                          description: |-
                            (String) Static IP configured on the new network interface.
                            Static IP configured on the new network interface.
                          type: string
                        name:
                          description: |-
                            (String) The network interface name.
                            The network interface name.
                          type: string
                        subnetCidr:
                          description: |-
                            (String) The subnet CIDR for the network interface.
                            The subnet CIDR for the network interface.
                          type: string
                        vlan:
                          description: |-
                            (String) The VLAN for the network interface.
                            The VLAN for the network interface.
                          type: string
                      type: object
                    type: array
                  pinnedCores:
                    description: |-
                      (Number) List of host CPU cores to pin the VM host machine to. If this is passed, the cores parameter is ignored.
                      List of host CPU cores to pin the VM host machine to. If this is passed, the `cores` parameter is ignored.
                    type: number
                  pool:
                    description: |-
                      (String) The VM host machine pool. This is computed if it's not set.
                      The VM host machine pool. This is computed if it's not set.
                    type: string
                  storageDisks:
                    description: |-
                      as-blocks mode. (see below for nested schema)
                      A list of storage disks for the new VM host. Parameters defined below. This argument is processed in [attribute-as-blocks mode](https://www.io/docs/configuration/attr-as-blocks.html).
                    items:
                      properties:
                        pool:
                          description: |-
                            (String) The VM host machine pool. This is computed if it's not set.
                            The VM host storage pool name.
                          type: string
                        sizeGigabytes:
                          description: |-
                            (Number) The storage disk size, specified in GB.
                            The storage disk size, specified in GB.
                          type: number
                      type: object
                    type: array
                  vmHost:
                    description: |-
                      (String) ID or name of the VM host used to compose the new machine.
                      ID or name of the VM host used to compose the new machine.
                    type: string
                  vmHostRef:
                    description: Reference to a VMHost in machine to populate vmHost.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  vmHostSelector:
                    description: Selector for a VMHost in machine to populate vmHost.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  zone:
                    description: |-
                      (String) The VM host machine zone. This is computed if it's not set.
                      The VM host machine zone. This is computed if it's not set.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: VMHostMachineStatus defines the observed state of VMHostMachine.
            properties:
              atProvider:
                properties:
                  cores:
                    description: |-
                      (Number) The number of CPU cores (defaults to 1).
                      The number of CPU cores (defaults to 1).
                    type: number
                  domain:
                    description: |-
                      (String) The VM host machine domain. This is computed if it's not set.
                      The VM host machine domain. This is computed if it's not set.
                    type: string
                  hostname:
                    description: |-
                      (String) The VM host machine hostname. This is computed if it's not set.
                      The VM host machine hostname. This is computed if it's not set.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  memory:
                    description: |-
                      (Number) The VM host machine RAM memory, specified in MB (defaults to 2048).
                      The VM host machine RAM memory, specified in MB (defaults to 2048).
                    type: number
                  networkInterfaces:
                    description: |-
                      as-blocks mode. (see below for nested schema)
                      A list of network interfaces for new the VM host. This argument only works when the VM host is deployed from a registered MAAS machine. Parameters defined below. This argument is processed in [attribute-as-blocks mode](https://www.io/docs/configuration/attr-as-blocks.html).
                    items:
                      properties:
                        fabric:
                          description: |-
                            (String) The fabric for the network interface.
                            The fabric for the network interface.
                          type: string
                        ipAddress:
                          description: |-
                            (String) Static IP configured on the new network interface.
                            Static IP configured on the new network interface.
                          type: string
                        name:
                          description: |-
                            (String) The network interface name.
                            The network interface name.
                          type: string
                        subnetCidr:
                          description: |-
                            (String) The subnet CIDR for the network interface.
                            The subnet CIDR for the network interface.
                          type: string
                        vlan:
                          description: |-
                            (String) The VLAN for the network interface.
                            The VLAN for the network interface.
                          type: string
                      type: object
                    type: array
                  pinnedCores:
                    description: |-
                      (Number) List of host CPU cores to pin the VM host machine to. If this is passed, the cores parameter is ignored.
                      List of host CPU cores to pin the VM host machine to. If this is passed, the `cores` parameter is ignored.
                    type: number
                  pool:
                    description: |-
                      (String) The VM host machine pool. This is computed if it's not set.
                      The VM host machine pool. This is computed if it's not set.
                    type: string
                  storageDisks:
                    description: |-
                      as-blocks mode. (see below for nested schema)
                      A list of storage disks for the new VM host. Parameters defined below. This argument is processed in [attribute-as-blocks mode](https://www.io/docs/configuration/attr-as-blocks.html).
                    items:
                      properties:
                        pool:
                          description: |-
                            (String) The VM host machine pool. This is computed if it's not set.
                            The VM host storage pool name.
                          type: string
                        sizeGigabytes:
                          description: |-
                            (Number) The storage disk size, specified in GB.
                            The storage disk size, specified in GB.
                          type: number
                      type: object
                    type: array
                  vmHost:
                    description: |-
                      (String) ID or name of the VM host used to compose the new machine.
                      ID or name of the VM host used to compose the new machine.
                    type: string
                  zone:
                    description: |-
                      (String) The VM host machine zone. This is computed if it's not set.
                      The VM host machine zone. This is computed if it's not set.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
