---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: blockdevices.storage.maas.m.crossplane.io
spec:
  group: storage.maas.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - maas
    kind: BlockDevice
    listKind: BlockDeviceList
    plural: blockdevices
    singular: blockdevice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BlockDevice is the Schema for the BlockDevices API. Provides
          a resource to manage MAAS machines' block devices.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BlockDeviceSpec defines the desired state of BlockDevice
            properties:
              forProvider:
                properties:
                  blockSize:
                    description: |-
                      (Number) The block size of the block device. Defaults to 512.
                      The block size of the block device. Defaults to `512`.
                    type: number
                  idPath:
                    description: |-
                      (String) Only used if model and serial cannot be provided. This should be a path that is fixed and doesn't change depending on the boot order or kernel version. This argument is computed if it's not given.
                      Only used if `model` and `serial` cannot be provided. This should be a path that is fixed and doesn't change depending on the boot order or kernel version. This argument is computed if it's not given.
                    type: string
                  isBootDevice:
                    description: |-
                      (Boolean) Boolean value indicating if the block device is set as the boot device.
                      Boolean value indicating if the block device is set as the boot device.
                    type: boolean
                  machine:
                    description: |-
                      (String) The machine identifier (system ID, hostname, or FQDN) that owns the block device.
                      The machine identifier (system ID, hostname, or FQDN) that owns the block device.
                    type: string
                  machineRef:
                    description: Reference to a Machine in machine to populate machine.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  machineSelector:
                    description: Selector for a Machine in machine to populate machine.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  model:
                    description: |-
                      (String) Model of the block device. Used in conjunction with serial argument. Conflicts with id_path. This argument is computed if it's not given.
                      Model of the block device. Used in conjunction with `serial` argument. Conflicts with `id_path`. This argument is computed if it's not given.
                    type: string
                  name:
                    description: |-
                      (String) The block device name.
                      The block device name.
                    type: string
                  partitions:
                    description: |-
                      as-blocks mode. And, it is computed if it's not given. (see below for nested schema)
                      List of partition resources created for the new block device. Parameters defined below. This argument is processed in [attribute-as-blocks mode](https://www.io/docs/configuration/attr-as-blocks.html). And, it is computed if it's not given.
                    items:
                      properties:
                        bootable:
                          description: |-
                            (Boolean) Boolean value indicating if the partition is set as bootable.
                            Boolean value indicating if the partition is set as bootable.
                          type: boolean
                        fsType:
                          description: |-
                            (String) The file system type (e.g. ext4). If this is not set, the partition is unformatted.
                            The file system type (e.g. `ext4`). If this is not set, the partition is unformatted.
                          type: string
                        label:
                          description: |-
                            (String) The label assigned if the partition is formatted.
                            The label assigned if the partition is formatted.
                          type: string
                        mountOptions:
                          description: |-
                            (String) The options used for the partition mount.
                            The options used for the partition mount.
                          type: string
                        mountPoint:
                          description: |-
                            (String) The mount point used. If this is not set, the partition is not mounted. This is used only the partition is formatted.
                            The mount point used. If this is not set, the partition is not mounted. This is used only the partition is formatted.
                          type: string
                        sizeGigabytes:
                          description: |-
                            (Number) The size of the block device (given in GB).
                            The partition size (given in GB).
                          type: number
                        tags:
                          description: |-
                            (Set of String) A set of tag names assigned to the new block device. This argument is computed if it's not given.
                            The tags assigned to the new block device partition.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  serial:
                    description: |-
                      (String) Serial number of the block device. Used in conjunction with model argument. Conflicts with id_path. This argument is computed if it's not given.
                      Serial number of the block device. Used in conjunction with `model` argument. Conflicts with `id_path`. This argument is computed if it's not given.
                    type: string
                  sizeGigabytes:
                    description: |-
                      (Number) The size of the block device (given in GB).
                      The size of the block device (given in GB).
                    type: number
                  tags:
                    description: |-
                      (Set of String) A set of tag names assigned to the new block device. This argument is computed if it's not given.
                      A set of tag names assigned to the new block device. This argument is computed if it's not given.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  blockSize:
                    description: |-
                      (Number) The block size of the block device. Defaults to 512.
                      The block size of the block device. Defaults to `512`.
                    type: number
                  idPath:
                    description: |-
                      (String) Only used if model and serial cannot be provided. This should be a path that is fixed and doesn't change depending on the boot order or kernel version. This argument is computed if it's not given.
                      Only used if `model` and `serial` cannot be provided. This should be a path that is fixed and doesn't change depending on the boot order or kernel version. This argument is computed if it's not given.
                    type: string
                  isBootDevice:
                    description: |-
                      (Boolean) Boolean value indicating if the block device is set as the boot device.
                      Boolean value indicating if the block device is set as the boot device.
                    type: boolean
                  machine:
                    description: |-
                      (String) The machine identifier (system ID, hostname, or FQDN) that owns the block device.
                      The machine identifier (system ID, hostname, or FQDN) that owns the block device.
                    type: string
                  machineRef:
                    description: Reference to a Machine in machine to populate machine.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  machineSelector:
                    description: Selector for a Machine in machine to populate machine.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  model:
                    description: |-
                      (String) Model of the block device. Used in conjunction with serial argument. Conflicts with id_path. This argument is computed if it's not given.
                      Model of the block device. Used in conjunction with `serial` argument. Conflicts with `id_path`. This argument is computed if it's not given.
                    type: string
                  name:
                    description: |-
                      (String) The block device name.
                      The block device name.
                    type: string
                  partitions:
                    description: |-
                      as-blocks mode. And, it is computed if it's not given. (see below for nested schema)
                      List of partition resources created for the new block device. Parameters defined below. This argument is processed in [attribute-as-blocks mode](https://www.io/docs/configuration/attr-as-blocks.html). And, it is computed if it's not given.
                    items:
                      properties:
                        bootable:
                          description: |-
                            (Boolean) Boolean value indicating if the partition is set as bootable.
                            Boolean value indicating if the partition is set as bootable.
                          type: boolean
                        fsType:
                          description: |-
                            (String) The file system type (e.g. ext4). If this is not set, the partition is unformatted.
                            The file system type (e.g. `ext4`). If this is not set, the partition is unformatted.
                          type: string
                        label:
                          description: |-
                            (String) The label assigned if the partition is formatted.
                            The label assigned if the partition is formatted.
                          type: string
                        mountOptions:
                          description: |-
                            (String) The options used for the partition mount.
                            The options used for the partition mount.
                          type: string
                        mountPoint:
                          description: |-
                            (String) The mount point used. If this is not set, the partition is not mounted. This is used only the partition is formatted.
                            The mount point used. If this is not set, the partition is not mounted. This is used only the partition is formatted.
                          type: string
                        sizeGigabytes:
                          description: |-
                            (Number) The size of the block device (given in GB).
                            The partition size (given in GB).
                          type: number
                        tags:
                          description: |-
                            (Set of String) A set of tag names assigned to the new block device. This argument is computed if it's not given.
                            The tags assigned to the new block device partition.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  serial:
                    description: |-
                      (String) Serial number of the block device. Used in conjunction with model argument. Conflicts with id_path. This argument is computed if it's not given.
                      Serial number of the block device. Used in conjunction with `model` argument. Conflicts with `id_path`. This argument is computed if it's not given.
                    type: string
                  sizeGigabytes:
                    description: |-
                      (Number) The size of the block device (given in GB).
                      The size of the block device (given in GB).
                    type: number
                  tags:
                    description: |-
                      (Set of String) A set of tag names assigned to the new block device. This argument is computed if it's not given.
                      A set of tag names assigned to the new block device. This argument is computed if it's not given.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.sizeGigabytes is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.sizeGigabytes)
                || (has(self.initProvider) && has(self.initProvider.sizeGigabytes))'
          status:
            description: BlockDeviceStatus defines the observed state of BlockDevice.
            properties:
              atProvider:
                properties:
                  blockSize:
                    description: |-
                      (Number) The block size of the block device. Defaults to 512.
                      The block size of the block device. Defaults to `512`.
                    type: number
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  idPath:
                    description: |-
                      (String) Only used if model and serial cannot be provided. This should be a path that is fixed and doesn't change depending on the boot order or kernel version. This argument is computed if it's not given.
                      Only used if `model` and `serial` cannot be provided. This should be a path that is fixed and doesn't change depending on the boot order or kernel version. This argument is computed if it's not given.
                    type: string
                  isBootDevice:
                    description: |-
                      (Boolean) Boolean value indicating if the block device is set as the boot device.
                      Boolean value indicating if the block device is set as the boot device.
                    type: boolean
                  machine:
                    description: |-
                      (String) The machine identifier (system ID, hostname, or FQDN) that owns the block device.
                      The machine identifier (system ID, hostname, or FQDN) that owns the block device.
                    type: string
                  model:
                    description: |-
                      (String) Model of the block device. Used in conjunction with serial argument. Conflicts with id_path. This argument is computed if it's not given.
                      Model of the block device. Used in conjunction with `serial` argument. Conflicts with `id_path`. This argument is computed if it's not given.
                    type: string
                  name:
                    description: |-
                      (String) The block device name.
                      The block device name.
                    type: string
                  partitions:
                    description: |-
                      as-blocks mode. And, it is computed if it's not given. (see below for nested schema)
                      List of partition resources created for the new block device. Parameters defined below. This argument is processed in [attribute-as-blocks mode](https://www.io/docs/configuration/attr-as-blocks.html). And, it is computed if it's not given.
                    items:
                      properties:
                        bootable:
                          description: |-
                            (Boolean) Boolean value indicating if the partition is set as bootable.
                            Boolean value indicating if the partition is set as bootable.
                          type: boolean
                        fsType:
                          description: |-
                            (String) The file system type (e.g. ext4). If this is not set, the partition is unformatted.
                            The file system type (e.g. `ext4`). If this is not set, the partition is unformatted.
                          type: string
                        label:
                          description: |-
                            (String) The label assigned if the partition is formatted.
                            The label assigned if the partition is formatted.
                          type: string
                        mountOptions:
                          description: |-
                            (String) The options used for the partition mount.
                            The options used for the partition mount.
                          type: string
                        mountPoint:
                          description: |-
                            (String) The mount point used. If this is not set, the partition is not mounted. This is used only the partition is formatted.
                            The mount point used. If this is not set, the partition is not mounted. This is used only the partition is formatted.
                          type: string
                        path:
                          description: |-
                            (String) Block device path.
                            The path of the partition.
                          type: string
                        sizeGigabytes:
                          description: |-
                            (Number) The size of the block device (given in GB).
                            The partition size (given in GB).
                          type: number
                        tags:
                          description: |-
                            (Set of String) A set of tag names assigned to the new block device. This argument is computed if it's not given.
                            The tags assigned to the new block device partition.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  path:
                    description: |-
                      (String) Block device path.
                      Block device path.
                    type: string
                  serial:
                    description: |-
                      (String) Serial number of the block device. Used in conjunction with model argument. Conflicts with id_path. This argument is computed if it's not given.
                      Serial number of the block device. Used in conjunction with `model` argument. Conflicts with `id_path`. This argument is computed if it's not given.
                    type: string
                  sizeGigabytes:
                    description: |-
                      (Number) The size of the block device (given in GB).
                      The size of the block device (given in GB).
                    type: number
                  tags:
                    description: |-
                      (Set of String) A set of tag names assigned to the new block device. This argument is computed if it's not given.
                      A set of tag names assigned to the new block device. This argument is computed if it's not given.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  uuid:
                    description: |-
                      (String) Block device UUID.
                      Block device UUID.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
